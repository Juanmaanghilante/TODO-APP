import React from "react";

const CreateToDoButton = () => {
 
  return (
    <button
      className="button1"
      onClick={(event) => {
       
       
       
     
      }}
    >
      Add new task
    </button>
  );
};

export default CreateToDoButton;
